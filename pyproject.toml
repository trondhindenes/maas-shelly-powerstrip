[project]
name = "maas-shelly-powerstrip"
version = "0.1.0"
description = "Power-control webservice for Ubuntu MAAS backed by a Shelly Power Strip (Gen2+ RPC API)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "httpx>=0.27",
    "jinja2>=3.1.6",
    "pydantic-settings>=2.4",
    "python-multipart>=0.0.32",
    "uvicorn[standard]>=0.30",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.24",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/maas_shelly_powerstrip"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
